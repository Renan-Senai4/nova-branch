// import { FoodCard } from "./FoodCard";

// export function Dashboard({ filteredFood, onAddToFavorites, favorites }) {
//   return (
//     <div className="vortex-grid">
//       {filteredFood.length > 0 ? (
//         filteredFood.map((g) => (
//            <FoodCard
//     key={product.id}
//     title={product.title}
//     category={product.category}
//     banner={product.banner}
//     index={index}
//     isFavorite={favorites.map(String).includes(String(product.id))}
//     onFavorite={() => toggleFavorite(product.id)}
//     onPlay={() => setSelectedFood(product)}
//     showFavorite={false}           // esconder no dashboard
//   />
//         ))
//       ) : (
//         <p style={{ color: "#94a3b8" }}>Nenhum Pedido Feito..</p>
//       )}
//     </div>
//   );
// }
